# Lunatic Craft slot defaults. Loaded server-side and synced to clients.
# Keep room for accessory-heavy progression without making every Curio stack freely.
slots = ["id=ring;size=6", "id=charm;size=4"]
